export type TDimensao = {
  x: number;
  y: number;
};

export type TCoord = {
  x: number;
  y: number;
};

export type TDeslocamento = string | 'F' | 'T' | 'D' | 'E';

export type TOrientacao = 'N' | 'S' | 'L' | 'O';
